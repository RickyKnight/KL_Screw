<br>
<div>
  <%= f.label :content, "Question" %> </br>
  <%= f.text_area :content, :rows => 3 %> </br>
  <%= f.input :question_type, collection: {"checkbox"=>1, "radio buttons" =>2, "text feild" =>3}, prompt: false %>
  <%= f.link_to_remove "remove question" %> 
</div> </br>

<%= f.fields_for :options do |builder| %>
  <%= render '/jobs/question_option_fields', :f => builder %>
<% end %> 
<%= f.link_to_add "Add Option", :options %> 