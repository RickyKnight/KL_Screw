<h1>Admin#index</h1>
<p>Find me in app/views/admin/index.html.erb</p> </br>


<%= simple_form_for Application.new do |f| %>
  <div class="field">
    <%= f.collection_select :trade_id, Trade.order(:name), :id, :name, include_blank: true %>
  </div>

  <div class="field">
    <%= f.grouped_collection_select :job_id, Trade.order(:name), :jobs, :name, :id, :name, include_blank: true %>
  </div>

  <div class="actions"><%= f.submit "Edit From" %></div>
<% end %> </br>


<%= form_tag new_job_path, method: :get do %>
  <%= select_tag :trade_id, options_from_collection_for_select(Trade.all, :id, :name, include_blank: true) %>
  <%= submit_tag 'New Job', name: nil %>
<% end %> </br>
